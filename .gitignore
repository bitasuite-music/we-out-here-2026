# macOS
.DS_Store

# Working notes for AI coding sessions. Deliberately not tracked: Cloudflare
# Pages serves the repo root as static files, so anything committed here is
# publicly readable at melomaniacs.pages.dev/<filename>.
CLAUDE.md
CLAUDE.local.md

# Build tools live outside this repo (../tools/) because Pages serves the repo
# root publicly. Listed here so a stray copy can never be committed back in.
check-updates.py
build_pdfs.py
extract_data.py

# Build scratch
*.raw.gif
__pycache__/
